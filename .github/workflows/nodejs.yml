# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# This workflow integrates njsscan with GitHub's Code Scanning feature
# nodejsscan is a static security code scanner that finds insecure code patterns in your Node.js applications

name: njsscan sarif

on:
  push:
    branches: [ "main" ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "main" ]

jobs:
  build:
    # định nghĩa môi trường mà job sẽ chạy
    # Remove folder node43 nếu folder đó đã tồn tại
    runs-on: self-hosted
    steps:
      - name: Clone source code from GitHub
        run: |
          cd node43_ci_cd/.github/workflows
          sudo chmod +x ./handler_source_be.sh
          ./handler_source_be.sh
      - name: Build MySQL
        run: |
          cd node43_ci_cd/.github/workflows
          sudo chmod +x ./build_mysql.sh
          ./build_mysql.sh
      - name: Build source BE
        run: |
          # stop và remove container đã tồn tại
          # remove image
          # build image mới
          # tạo container mới
          cd node43_ci_cd
          sudo docker build . -t node43
          sudo docker run -d -p 8080:8080 --name node43 node43

